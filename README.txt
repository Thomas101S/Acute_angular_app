It is preferred to use http-server/apache and copy the server link into your browser. If not, follow instructions in code.

npm: npm install http-server -g

homebrew: brew install http-server

If you made changes to the code and host it using http-server, you may have to hard-reload/clear cache in order to see new changes

If you add two duplicate items in the same list you will get an error

For importing scripts, type is always assumed to be js text so it is not neccessary to specify unless script isn't javascript. I included it bc sublime text auto-fills the bracket with it.

If anyone would like to turn the input line into a directive be my guest! Look at the button files (Might want to rename them)

When specifying an element in scope, make sure it has no special characters and is all lowercase
